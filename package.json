{
  "name": "guppy",
  "private": true,
  "scripts": {
    "build": "node scripts/build.mjs",
    "serve": "npm run build && node scripts/serve.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.11"
  }
}
